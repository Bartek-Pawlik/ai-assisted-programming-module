# Copy this file to .env and fill in your own key. .env is gitignored.
# Only the generation half of the lab (sections 3 to 5) uses it; chunking,
# embeddings and retrieval need no key at all.
ANTHROPIC_API_KEY=
