# Parts 1-5 run in the editor (GitHub Copilot); parts 6-8 are terminal
# agents installed with npm, not pip:
#   npm install -g @github/copilot
#   npm install -g @google/gemini-cli

# Running and covering the sample code (parts 1-3)
pytest>=7.4.0
pytest-cov>=4.1.0

# Part 4's authentication example
flask>=3.0.0
bcrypt>=4.0.1
pyjwt>=2.8.0
email-validator>=2.0.0

# Tools the agent is asked to satisfy in parts 2-3
black>=23.0.0
pylint>=3.0.0
mypy>=1.5.0
